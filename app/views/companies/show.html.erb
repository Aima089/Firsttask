<h1><%= @company.name %></h1>

<h1><%= @company.date %></h1>

<%= button_to "Destroy", company_path(@company), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %></li>
<%= button_to "New User", new_company_user_path(@company), method: :get  %>
<%= button_to "Users List", company_users_path(@company), method: :get %>
<%= button_to "Back to companies", companies_path, method: :get %>