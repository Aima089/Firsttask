<h1>Editing user</h1>

<%= render "form", user: @user %>

<br>
<ul>
  <li><%= link_to "Edit", edit_company_user_path(@company, @user) %></li>
  <li><%= link_to "Destroy", company_user_path(@company, @user), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>


<div>
  <%= link_to "Show this user", company_user_path(@company, @user) %> |
  <%= link_to "Back to users", company_users_path(@company) %>
</div>
